# openspec/config.yaml
version: "1.3"
schema: my-custom-workflow
specsPath: ./docs/technical-specs

# THIS replaces the old project.md
instructions: 
# - "The project uses a Microservices architecture"
- "All API definition must follow OpenAPI 3.0"
- "Security is a top priority; always check for SQL injection"